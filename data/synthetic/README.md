# Sample synthetic data

Generated by `nakshatra-synthetic-v1` (seed `20260921`), contract `1.0.0`,
covering `2023-09-21` to `2026-09-20`.

**Every row is synthetic.** `is_synthetic` is `true` throughout. These are not
MOIL's figures and must not be presented as measurements of MOIL's operations.
See `docs/INGESTION_CONTRACT.md`.

Regenerate with the rest of the artifact set, so everything shares one dataset:

```bash
cd backend && python -m app.api.batch all
```

Samples alone (same seed and end date) with `python -m app.ingestion.export`.

| Entity | Sample rows |
|---|---|
| `borehole` | 120 |
| `assay` | 500 |
| `lithology` | 500 |
| `production_by_mine_grade_period` | 500 |
| `equipment_event` | 500 |
| `blast_record` | 500 |
| `plan_target` | 500 |
